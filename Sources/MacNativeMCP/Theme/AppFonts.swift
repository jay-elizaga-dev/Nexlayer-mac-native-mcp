import SwiftUI

enum AppFonts {
    static let code      = Font.system(.body, design: .monospaced)
    static let codeSmall = Font.system(.caption, design: .monospaced)
    static let prose     = Font.system(.body, design: .default)
    static let label     = Font.system(.caption, weight: .medium)
    static let heading   = Font.system(.title3, weight: .semibold)
}
